[project]
name = "pipe-5291-proquest-observable"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.9"
dependencies = [
    "duckdb>=1.1.3",
    "fuzzywuzzy>=0.18.0",
    "geocoder>=1.38.1",
    "pyarrow>=18.1.0",
    "python-levenshtein>=0.26.1",
    "spacy>=3.8.3",
    "tqdm>=4.67.1",
]

[dependency-groups]
dev = [
    "duckdb>=1.1.3",
    "pip>=24.3.1",
    "pytest>=8.3.5",
]

[tool.pytest.ini_options]
pythonpath = [ "src/data" ]
